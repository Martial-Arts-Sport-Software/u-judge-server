# Правила работы с репозиторием

## Git и pull requests

1. Делите разработку на несколько небольших логических коммитов. Каждый коммит должен быть самостоятельным, понятным и проходить относящиеся к нему проверки.
2. Перед открытием pull request проверьте, что `README.md`, `docs/` и другие затронутые документы обновлены и точно отражают состояние после изменений.
3. После завершения работы и прохождения проверок открывайте pull request в целевую ветку. В описании PR укажите цель, ключевые изменения, проверки и известные ограничения.
4. Перед коммитом запускайте относящиеся к изменению Gradle-проверки, тесты и сборку; фиксируйте в PR выполненные команды и их результат.
5. Не коммитьте секреты, IDE-конфигурацию, локальные данные, каталоги сборки и generated artifacts, если их добавление не запрошено явно.
6. Не смешивайте в одном PR рефакторинг, изменение поведения и обновление зависимостей без явного обоснования в описании PR.
7. Используйте Conventional Commits: `<type>(<scope>): <imperative description>`, например `feat(pairing): add device approval flow`. Допустимые типы: `feat`, `fix`, `refactor`, `docs`, `test`, `build`, `ci`, `chore`.
8. Заголовок PR оформляйте по шаблону `<Type> | <Imperative Description>` в Title Case, например `Feat | Enforce Java 21 Validation`. `Type` соответствует типу Conventional Commit с первой заглавной буквы.
9. Планируйте PR как завершённый вертикальный slice: берите несколько связанных requirement IDs, их реализацию, тесты и документацию в один PR. Не объединяйте несвязанные требования только ради увеличения объёма PR.
10. Перед реализацией создавайте отдельную ветку от актуальной целевой ветки. Называйте её по шаблону `<type>/<short-kebab-scope>`, например `feat/device-pairing` или `fix/outbox-retry`; не выполняйте задачу напрямую в `main`.
11. После завершения задачи обновляйте `docs/ROADMAP.md` и связанные requirement statuses. Отмечайте выполненные пункты roadmap Markdown-чекбоксом `- [x]`, а незавершённые оставляйте как `- [ ]`; requirement IDs, этапы и gates отмечайте только при наличии тестов или другого зафиксированного доказательства приёмки.
12. Считайте локальный `main` актуальным. Перед созданием рабочей ветки переключайтесь на локальный `main`, но не выполняйте `git fetch`, `git pull` или другие сетевые Git-операции без явного запроса пользователя.
13. Для каждого PR сначала создавайте GitHub issue, связывайте её с подходящим milestone и указывайте связанные requirement IDs, scope и acceptance criteria.
14. В описание каждого PR добавляйте `Closes #<issue-number>`, например `Closes #20`, чтобы GitHub автоматически закрыл связанную issue после merge.
15. Перед созданием или обновлением issue и PR заполняйте GitHub metadata: назначайте `@TheGeniusOfEternity` assignee, подходящий milestone, один `type:*` label, релевантные `area:*` и `priority:*` labels, а для scope v1 - label `pilot`. Добавляйте issue и PR в подходящий GitHub Project; если Project не определён, спросите пользователя, а не создавайте новый самостоятельно.
